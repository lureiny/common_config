" 语法高亮
syntax on

" 文件类型检测
filetype on

" 不同文件不同缩进
filetype indent on

" 关闭vi模式
set nocp

" 允许使用插件
filetype plugin on
filetype plugin indent on

" 设置行号
set nu

" 文件改变时自动读取
set autoread

" 允许使用鼠标定位
set mouse=a

" 高亮光标所在行
set cursorline

" 总是显示状态行
set laststatus=2

" 状态栏显示当前执行的命令
set showcmd

" 搜索时，输入的语句逐字符高亮
set incsearch

" 粘贴时保持格式
set paste

" 继承前一行的缩进方式
set autoindent

" 为C提供自动缩进
set smartindent

" 使用C样式的suojin
set cindent

" 制表符为2空格
set tabstop=2

" 设置默认编码
set fenc=utf-8
